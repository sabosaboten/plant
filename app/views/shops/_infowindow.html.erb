<div>
  <%= "#{shop.name}"%>
  <%= link_to "投稿者詳細#{shop.user.name}", user_path(shop.user.id) %>
  <%= "#{shop.text}"%>
  <% if user_signed_in? && current_user.id == shop.user_id %> 
    <%= link_to "削除", shop_path(shop.id), method: :delete %>
    <%= link_to "編集", edit_shop_path(shop.id) %>  
  <% end %>
</div>