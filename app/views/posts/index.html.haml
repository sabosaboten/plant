-# %h3.header__name
-#   = current_user.name
-# = link_to "ショッピング", maps_index_path
-# = link_to new_schedule_path(current_user) do
-#   = icon('fas', 'pen-square', class: 'icon')
-# = link_to edit_user_path(current_user) do
-#   = icon('fas', 'cog', class: 'icon')
-# = link_to "投稿", new_post_path

-# .posts
-#   = render @posts

%h3.header__name
  = current_user.name
= link_to "ショッピング", maps_index_path
= link_to new_schedule_path(current_user) do
  = icon('fas', 'pen-square', class: 'icon')
= link_to edit_user_path(current_user) do
  = icon('fas', 'cog', class: 'icon')
= link_to "投稿", new_post_path

.h3 投稿一覧
.posts
  = render @posts